name: Code Review using CodeLlama

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:
  code_review:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v3
        with:
          fetch-depth: 0  # Fetch full history to ensure HEAD^ and HEAD~1 work

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.8'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install transformers sentencepiece torch huggingface_hub

      - name: Cache Hugging Face model
        uses: actions/cache@v3  # Updated to v3
        with:
          path: ~/.cache/huggingface  # Cache directory for Hugging Face models
          key: ${{ runner.os }}-transformers-${{ hashFiles('**/requirements.txt') }}  # Cache key based on requirements.txt or other unique changes
          restore-keys: |
            ${{ runner.os }}-transformers-

      - name: Get list of changed files
        id: files
        run: |
          if [[ $GITHUB_EVENT_NAME == "pull_request" ]]; then
            files=$(git diff --name-only ${{ github.event.pull_request.base.sha }} ${{ github.sha }})
          else
            files=$(git diff --name-only HEAD^ HEAD)
          fi
          echo "changed_files=$files" >> $GITHUB_ENV

      - name: Run code review with CodeLlama
        run: |
          python review_code.py "${{ env.changed_files }}"
